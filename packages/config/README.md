# @loyalflow/config

Reserved for typed names, defaults, and parsing contracts for non-secret configuration.
This placeholder contains no runtime code and does not change current environment loading.

Forbidden: secret values in client exports, database access, provider SDKs, and business
services.
