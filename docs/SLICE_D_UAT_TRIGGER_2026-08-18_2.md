# Slice D UAT trigger

Temporary branch-only marker to trigger the exact-SHA runtime UAT runner. It does not change staging, application runtime behavior, schema, environment configuration, or participant data.
